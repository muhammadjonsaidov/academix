/**
 * Compatibility entry point. New code imports from `@/components/shell/DashboardShell`;
 * keeping this export lets existing route modules migrate independently without a risky flag day.
 */
export { DashboardShell } from "@/components/shell/DashboardShell";
