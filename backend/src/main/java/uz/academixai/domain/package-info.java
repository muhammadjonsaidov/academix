/**
 * Entities, value objects, domain enums, and domain services. Pure business rules — no
 * Spring/JPA/framework dependencies. See CLAUDE.md "Repository layout" for the full
 * dependency-direction rule (infrastructure implements ports domain/application define, never the
 * reverse).
 */
package uz.academixai.domain;
