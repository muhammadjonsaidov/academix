/**
 * JPA entity mappings + repositories, RabbitMQ producers/consumers, external clients
 * (Qwen/Vision/Telegram/SeaweedFS), Redis adapters (AI budget counters, Bucket4j rate limiter).
 * Implements ports that {@code domain}/{@code application} define.
 */
package uz.academixai.infrastructure;
