/**
 * Use cases (application services) that orchestrate domain + ports. Request/response DTOs live at
 * this boundary. {@code create-entity}/{@code add-endpoint} skills target this layer alongside
 * {@code domain}/{@code interfaces.web}.
 */
package uz.academixai.application;
